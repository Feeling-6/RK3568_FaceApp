# ---------------------
# 1. 编译构建产物 (必须忽略)
# ---------------------
build/
bin/
*.o
*.obj
*.so
*.so.*
*.a
*.dll
*.dylib
*.exe

# ---------------------
# 2. Qt Creator IDE 生成文件 (由于你的截图里有，这个非常重要)
# ---------------------
# 用户特定的配置文件，绝对不能提交，否则同事打开会报错
CMakeLists.txt.user
CMakeLists.txt.user.*
*.pro.user
*.pro.user.*

# ---------------------
# 3. Qt 自动生成的文件 (MOC/UI)
# ---------------------
# 如果你使用了 CMake，这些通常在 build 目录里，
# 但为了防止意外在源码目录生成，建议加上
moc_*.cpp
ui_*.h
qrc_*.cpp
Makefile*

# ---------------------
# 4. 其他 IDE/编辑器 配置
# ---------------------
.vscode/
.idea/
.DS_Store
.claude/
CLAUDE.md

# ---------------------
# 5. 特殊文件处理 (AI 模型)
# ---------------------
# 如果 .rknn 模型非常大 (>100MB)，建议忽略并使用 Git LFS
# 如果不大，可以保留在 git 中，把下面这行注释掉
# *.rknn

# ---------------------
# 6. 临时文件
# ---------------------
*.log
*.tmp
*.png